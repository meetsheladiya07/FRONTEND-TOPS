DOM event flow describes how an event travels through elements, from the document down to the target element during capturing, then back up during bubbling. For example, in a Flipkart-style shopping cart, clicking an Add to Cart button inside a product card could also trigger a click handler on the parent card, causing the product to be added twice if both handlers add it to the cart.